1. ¿Qué diferencia hay entre sobrescritura (override) y sobrecarga
   (overload)? Da un ejemplo de cada una desde tu código.
- Con override, puedo cambiar como se comporta el metodo usando las mismas variables. Por ejemplo, tanto en mi
    practica anterior como en este, use el override en toString. Tecnicamente no le estoy pidiendo mas variables
    , solamente estoy modificando que va a imprimir con lo que ya tengo.
- Cosa diferente con overload/sobrecarga, en la que puedo pedir mas variables, osease, diferentes formas de trabajar
    con el mismo metodo
2. ¿Por qué usaste =instanceof= antes de hacer un cast? ¿Qué excepción
   previene?
- Me sirve para poder verificar si uno de mis objetos es parte de la clase que previamente escrita. Por ejemplo,
    corroborar que Valstrax y zombie pertenezcan a monstruo.
    necesariamente pertenecen a la misma familia o jerarquía.
3. ¿Podrías instanciar tu clase abstracta directamente? ¿Por qué sí o por
   qué no?